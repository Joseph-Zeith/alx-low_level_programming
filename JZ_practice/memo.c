#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
int main()
{
	int arr[1], n, j;
	printf("Enter the size of the array");
	printf("%d", &j);
	arr[1] = (int *)malloc(size of(int) * j);

	for (k = 0; k < 3; ++k)
	{
		printf("arr[%d]", k );
		scanf("%d", &arr1[k]);
	}
	printf("\n\n................. output ................... \n");
	for (k = 0; k < 3; ++k)
	{
		printf("arr1[%d] = %d\n", k, *arr1[k]);
	}
}
